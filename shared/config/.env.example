# GitHub
GITHUB_TOKEN=
GITHUB_REPO=Anoop-Kondepudi/hackai

# OpenAI (for task extraction via API — Phase 1)
OPENAI_API_KEY=

# Claude (for plan generation + code gen — Phase 2/3)
ANTHROPIC_API_KEY=

# Transcription (Shiv will fill in based on chosen service)
# DEEPGRAM_API_KEY=
ASSEMBLYAI_API_KEY=
# Or use local Whisper (no key needed)

# Supabase (if needed)
# SUPABASE_URL=
# SUPABASE_ANON_KEY=
