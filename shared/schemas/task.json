{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Task",
  "description": "An actionable task extracted from a transcript. Produced by pipeline/, consumed by backend/.",
  "type": "object",
  "required": ["id", "title", "description", "status"],
  "properties": {
    "id": {
      "type": "string",
      "description": "Unique task identifier"
    },
    "title": {
      "type": "string",
      "description": "Short task title for the GitHub issue"
    },
    "description": {
      "type": "string",
      "description": "Detailed task description extracted from conversation context"
    },
    "source_speaker": {
      "type": "string",
      "description": "Who mentioned or was assigned this task"
    },
    "source_text": {
      "type": "string",
      "description": "Original transcript text that generated this task"
    },
    "status": {
      "type": "string",
      "enum": ["extracted", "issue_created", "plan_posted", "approved", "pr_opened", "completed"],
      "description": "Current status in the pipeline"
    }
  }
}
